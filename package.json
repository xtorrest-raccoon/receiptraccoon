{
  "name": "receiptraccoon",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "eval": "pnpm --filter @rr/extraction run eval"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "turbo": "^2.10.5",
    "typescript": "^7.0.2"
  }
}
